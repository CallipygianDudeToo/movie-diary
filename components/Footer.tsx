import React from "react";

const Footer = () => (
    <footer className="w-full py-4 text-center text-sm text-gray-400 bg-transparent border-t border-t-gray-800 mt-8">
        <span> © {new Date().getFullYear()} - the movie diary</span>
    </footer>
);

export default Footer;
