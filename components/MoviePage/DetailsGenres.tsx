import React from 'react';
import { MovieProps } from './MovieProps';

const DetailsGenres = ({ movie } : MovieProps) => {
  return (
    <div>DetailsGenres</div>
  )
};

export default DetailsGenres;